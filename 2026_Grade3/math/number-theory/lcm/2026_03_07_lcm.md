# 🎓 Math & Code: The LCM-GCD Connection

### 1. The Definitions
* **GCD (Greatest Common Divisor):** The biggest number that divides both $a$ and $b$.
* **LCM (Least Common Multiple):** The smallest number that both $a$ and $b$ can multiply into.

---

### 2. The Property of LCM

Any **common multiple** of two numbers $a$ and $b$ must be a **multiple of their LCM**.

> **In simple terms:** If $M$ is a common multiple of $a$ and $b$, then $LCM(a, b)$ can perfectly divide $M$.
> $$LCM(a, b) \mid M$$

---

###  Example 
Let’s use $a = 2$ and $b = 5$:
* **LCM(2, 5) = 10**
* **Other Common Multiples:** 20, 30, 40, 50...
* **The Connection:** Notice that 20, 30, 40, and 50 are all **multiples of 10**! 
* You cannot find a common multiple of 2 and 5 (like 15 or 25) that 10 cannot divide.

---

### **✍️ Quick Test**
Given $\text{lcm}(a, b) = 15$:
* Is $45$ a common multiple? **Yes**, because $15 \mid 45$.
* Is $50$ a common multiple? **No**, because $15 \nmid 50$.

### **💻 Python Proof Snippet**
```python
# Theorem 1 Check
L = lcm(a, b)
M = a * b * 3  # M is definitely a common multiple
print(M % L == 0) # This will ALWAYS be True
```

### 3. The Magical Formula
For any two positive integers $a$ and $b$:
$$LCM(a, b) = \frac{a \times b}{GCD(a, b)}$$

---

### The "Lego" Proof (Why it works)
Imagine every number is built from **Prime Factor** blocks. 
Let's use $a=12$ and $b=18$:

* **Blocks for 12:** $\{2, 2, 3\}$
* **Blocks for 18:** $\{2, 3, 3\}$

#### The Discovery:
1.  **The Shared Blocks (GCD):** Both numbers share one `2` and one `3`. 
    * $GCD = 2 \times 3 = 6$.
2.  **The Multiplication ($a \times b$):** If you multiply them, you get ALL the blocks: 
    * $\{2, 2, 3\}$ + $\{2, 3, 3\} = \{2, 2, 2, 3, 3, 3\}$.
    * *Notice:* The shared blocks ($2, 3$) appear **twice**!
3.  **The LCM:** To build the LCM, we only need the shared blocks **once**.
    * **Logic:** $LCM = \frac{\text{All Blocks}}{\text{Shared Blocks Once}}$

---

### 4: The "Nested" LCM Rule (Theorem)
This theorem shows how to find the LCM of **three** numbers by doing it in two steps.

**The Theory:**
To find $LCM(a_1, a_2, a_3)$:
1. First, find the LCM of the first two numbers: $m_2 = LCM(a_1, a_2)$.
2. Then, find the LCM of that result and the third number: $m_3 = LCM(m_2, a_3)$.
3. **Conclusion:** $m_3$ is the $LCM(a_1, a_2, a_3)$.

**The Proof (Why it works):**
* Because $m_3$ is a multiple of $m_2$, and $m_2$ is a multiple of $a_1$ and $a_2$, then $m_3$ is a common multiple of **all three** ($a_1, a_2, a_3$).
* Any other common multiple $M$ of $a_1, a_2, a_3$ must be a multiple of $m_2$ (by our previous rule).
* Since $M$ is also a multiple of $a_3$, it must be a multiple of $LCM(m_2, a_3)$, which is $m_3$.
* Therefore, $m_3$ is the **Least** Common Multiple.

---

### 5. Python Implementation
We can calculate LCM using his existing GCD function:

```python
def gcd(a, b):
    while b!=0:
        a, b = b, a % b
    return a

def lcm(a, b):
    # Apply the Magical Formula
    return (a * b) // gcd(a, b)

# Test it!
print(lcm(12, 18))  # Output: 36
```



### ✍️ Practice Challenge


#### 📝 Part 1: Step-by-Step Calculation

| Task | Problem | Mathematical Working | Result |
| :--- | :--- | :--- | :--- |
| **Q1** | Find the **GCD** of $(10, 15)$ | Factors of 10: {1, 2, **5**, 10}<br>Factors of 15: {1, 3, **5**, 15} | **5** |
| **Q2** | Calculate the **Product** | $10 \times 15$ | **150** |
| **Q3** | Use the **LCM Formula** | $\text{LCM} = \frac{10 \times 15}{\text{GCD}(10, 15)} = \frac{150}{5}$ | **30** |
| **Q4** | **Coprime Rule** | If $\text{GCD}(a, b) = 1$, then $\text{LCM} = \dots$ | **$a \times b$** |



---

#### 🧩 Part 2: The LCM Chain Challenge (Sequential Logic)

**Q5: If $LCM(10, 15) = 30$, how do you find $LCM(10, 15, 20)$?**

* **(A) Find $LCM(30, 20)$**
    * *Logic: Combine the LCM of the first two numbers with the third number.*
* **(B) Multiply $10 \times 15 \times 20$**
    * *Logic: This only works if all three numbers are pairwise coprime; here, the result would be way too large.*
* **(C) Just pick the biggest number (20)**
    * *Logic: This only works if 20 is a multiple of both 10 and 15.*



---

#### 🔑 Answer Key & Logic Analysis

| Question | Answer | Mathematical Reasoning |
| :--- | :--- | :--- |
| **Q1** | **5** | 5 is the largest positive integer that divides both 10 and 15 without a remainder. |
| **Q2** | **150** | The product of the two original terms. |
| **Q3** | **30** | Verification: <br>Multiples of 10: 10, 20, **30**... <br>Multiples of 15: 15, **30**... |
| **Q4** | **$a \times b$** | When numbers are coprime (GCD = 1), the denominator is 1, so the LCM is simply their product. |
| **Q5** | **(A)** | **Associative Property:** $LCM(a, b, c) = LCM(LCM(a, b), c)$. <br>Calculation: $LCM(30, 20) = 60$. |

